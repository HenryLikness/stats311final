The .csv file is the raw data

The .pptx file is the powerpoint presentation regarding the report

The .pdf is the report on the data

The .jmp is the script that recorded the data manipulation in JMP 
